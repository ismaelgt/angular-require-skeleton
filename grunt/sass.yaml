dev:
  files:
     '<%= dev.root %>/css/main.css' : '<%= src.root %>/scss/main.scss'
  options:
    style: 'expanded'
    sourcemap: 'file'

build:
  files:
     '<%= build.root %>/css/main.css' : '<%= src.root %>/scss/main.scss'
  options:
    style: 'compressed'
    sourcemap: 'none'
